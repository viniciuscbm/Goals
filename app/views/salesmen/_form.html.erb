<%= form_with(model: salesman, local: true) do |form| %>
  <% if salesman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(salesman.errors.count, "error") %> prohibited this salesman from being saved:</h2>

      <ul>
      <% salesman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.collection_select(:store_id, @stores, :id, :name, {}, {}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

